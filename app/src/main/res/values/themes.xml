<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AdoteUmaÁrvore" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/baseGreen</item>
        <item name="colorPrimaryDark">@color/midGreen</item>
        <item name="android:windowBackground">@color/baseBeige</item>
    </style>

    <style name="Theme.AdoteUmaÁrvore" parent="Base.Theme.AdoteUmaÁrvore" />

    <style name="Theme.AdoteUmaÁrvore.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AdoteUmaÁrvore.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AdoteUmaÁrvore.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>